apiVersion: v1
kind: Service
metadata:
  name: payflow
  namespace: payflow
spec:
  type: ClusterIP

  selector:
    app: payflow

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080